import { extendTheme } from '@mui/joy/styles';

export const defaultTheme = extendTheme({
    colorSchemes: {
        light: {
            palette: {
                primary: undefined,
            },
        },
        dark: {
            palette: {
                primary: undefined,
            },
        },
    },
})